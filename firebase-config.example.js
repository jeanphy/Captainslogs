/**
 * Firebase Configuration Template — The Sanctuary
 *
 * 1. Copy this file and rename it to: firebase-config.js
 * 2. Fill in your values from Firebase Console:
 *    Project Settings → Your Apps → Firebase SDK snippet
 * 3. firebase-config.js is in .gitignore and will NOT be committed.
 */

const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT.firebasestorage.app",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};

export default firebaseConfig;
